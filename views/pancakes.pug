extends layout

block content
  h1= title
  - var pancakes = results
  div(class="container-fluid")
  div(class="row")
   each i,index  in pancakes
    div(class="col-sm-2" onclick =`pancakes(${index})`) #{i.pancake_topping}, #{i.size}, #{i.cost}
     script.
        function pancakes(i){
            alert(`you selected pancakes ${i+1}`)
        }